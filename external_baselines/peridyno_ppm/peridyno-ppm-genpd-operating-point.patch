diff --git a/src/Dynamics/Cuda/Peridynamics/Module/CoSemiImplicitHyperelasticitySolver.cu b/src/Dynamics/Cuda/Peridynamics/Module/CoSemiImplicitHyperelasticitySolver.cu
--- a/src/Dynamics/Cuda/Peridynamics/Module/CoSemiImplicitHyperelasticitySolver.cu
+++ b/src/Dynamics/Cuda/Peridynamics/Module/CoSemiImplicitHyperelasticitySolver.cu
@@ -1034,21 +1034,22 @@ namespace dyno
 				y_current.assign(y_next);
 			}
 
-			// do Jacobi method Loop
-			mContactRule->inNewPosition()->assign(y_next);
-			mContactRule->initCCDBroadPhase();
-
-			mContactRule->inNewPosition()->assign(this->mPosBuf);
-			convergeFlag = false; // converge or not
-			iterCount = 0;
-			alpha = 1.0f;
-			max_grad_mag = 1e3;
+			if (this->selfContact) {
+				mContactRule->inNewPosition()->assign(y_next);
+				mContactRule->initCCDBroadPhase();
+
+				mContactRule->inNewPosition()->assign(this->mPosBuf);
+				convergeFlag = false; // converge or not
+				iterCount = 0;
+				alpha = 1.0f;
+				max_grad_mag = 1e3;
+				mPosBuf_March.assign(this->mPosBuf);
+			}
 	}
-		mPosBuf_March.assign(this->mPosBuf);
 
 
-		auto& cntPos = mContactRule->inNewPosition()->getData();
-		while (iterCount < this->varIterationNumber()->getData() && max_grad_mag >= this->grad_res_eps) {
+		auto& cntPos = this->selfContact ? mContactRule->inNewPosition()->getData() : y_next;
+		while (this->selfContact && iterCount < this->varIterationNumber()->getData() && max_grad_mag >= this->grad_res_eps) {
 			m_source.reset();
 			m_A.reset();
 			y_current.assign(cntPos);
diff --git a/src/Rendering/GUI/GlfwGUI/GlfwRenderWindow.h b/src/Rendering/GUI/GlfwGUI/GlfwRenderWindow.h
--- a/src/Rendering/GUI/GlfwGUI/GlfwRenderWindow.h
+++ b/src/Rendering/GUI/GlfwGUI/GlfwRenderWindow.h
@@ -26,6 +26,8 @@ namespace dyno {
         void initialize(int width, int height) override;
 
         void mainLoop() override;
+		bool renderOneFrame();
+		void captureFrame(const std::string& filename);
 
 		const std::string& name() const;
 
diff --git a/src/Rendering/GUI/GlfwGUI/GlfwRenderWindow.cpp b/src/Rendering/GUI/GlfwGUI/GlfwRenderWindow.cpp
--- a/src/Rendering/GUI/GlfwGUI/GlfwRenderWindow.cpp
+++ b/src/Rendering/GUI/GlfwGUI/GlfwRenderWindow.cpp
@@ -191,6 +191,33 @@ namespace dyno
 		style.PopupRounding = 6.0f;
 	}
 
+	bool GlfwRenderWindow::renderOneFrame()
+	{
+		auto activeScene = SceneGraphFactory::instance()->active();
+		glfwPollEvents();
+		activeScene->updateGraphicsContext();
+
+		mRenderParams.width = mCamera->viewportWidth();
+		mRenderParams.height = mCamera->viewportHeight();
+		if (mRenderParams.width == 0 || mRenderParams.height == 0)
+			return false;
+
+		mRenderParams.transforms.model = glm::mat4(1);
+		mRenderParams.transforms.view = mCamera->getViewMat();
+		mRenderParams.transforms.proj = mCamera->getProjMat();
+		mRenderParams.unitScale = mCamera->unitScale();
+		mRenderParams.groundPlaneAxis = (int)mCamera->viewportType();
+
+		mRenderEngine->draw(activeScene.get(), mRenderParams);
+		glfwSwapBuffers(mWindow);
+		return true;
+	}
+
+	void GlfwRenderWindow::captureFrame(const std::string& filename)
+	{
+		onSaveScreen(filename);
+	}
+
 	void GlfwRenderWindow::mainLoop()
 	{
 	
